name: Daily Event Automation

on:
  schedule:
    # Runs daily at midnight UTC
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-all-events:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install cloudscraper beautifulsoup4

      - name: Clone confs.tech data
        run: |
          git clone --depth 1 https://github.com/tech-conferences/conference-data.git temp_confs_data

      - name: Run Fetchers
        run: |
          python fetchers/bigevent.py
          python fetchers/developer_events.py
          python fetchers/cons_tech.py

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md
          
          if ! git diff --staged --quiet; then
            git commit -m "chore: auto-update all events"
            git push
          else
            echo "No new events or changes found. Skipping commit."
          fi
